# Optional ZAP Baseline configuration
# See: https://www.zaproxy.org/docs/desktop/addons/baseline-scan/
# You can ignore or warn on specific rules here to reduce noise during baseline scans.

# Example: ignore missing anti-CSRF tokens on GET endpoints (rule 10202)
10202 IGNORE

# Example: ignore X-Content-Type-Options header not set (rule 10021) for baseline
10021 WARN
